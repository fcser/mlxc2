<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jxxy.mlxc.auth.mapper.UserDAO">

	<resultMap id="userMap" type="com.jxxy.mlxc.auth.api.dto.UserDto">
		<result column="id" jdbcType="INTEGER" property="id"/>
		<result column="phone" jdbcType="VARCHAR" property="phone"/>
		<result column="password" jdbcType="VARCHAR" property="password"/>
		<result column="user_name" jdbcType="VARCHAR" property="userName"/>
		<result column="sex" jdbcType="INTEGER" property="sex"/>
		<result column="age" jdbcType="INTEGER" property="age"/>
		<result column="signature" jdbcType="VARCHAR" property="signature"/>
		<result column="roles" jdbcType="INTEGER" property="role"/>
		<result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
	</resultMap>
	<!-- <cache type="cn.myhexin.hwfour.redis.RedisCache"/> -->
	<insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="user">
	    insert into users(phone,password,user_name,sex,age,signature,roles) values(#{phone},#{password},#{userName},#{sex},#{age},#{signature},#{role})
	</insert>
	
	<update id="update" parameterType="user">
	    update users set 
	    <if test="userName!=null and userName!=''">
	    	user_name=#{userName},
	    </if>
	    <if test="sex!=null and sex!=''">
	    	sex=#{sex},
	    </if>
	    <if test="phone!=null and phone!=''">
	    	phone=#{phone},
	    </if>
	    <if test="password!=null and password!=''">
	    	password=#{password},
	    </if>
	    <if test="age!=null and age!=''">
	    	age=#{age},
	    </if>
	    <if test="signature!=null and signature!=''">
	    	signature=#{signature),
	    </if>
	    <if test="role!=null and role!=''">
	    	roles=#{role} 
	    </if> where id=#{id}
	</update>
	
	<delete id="delete" parameterType="Long">
		delete from users where id=#{id}
	</delete> 
	<!-- 用于登录 -->
	<select id="getUserByPhone" resultMap="userMap" parameterType="String">
		select * from users where phone=#{phone}
	</select>
	
</mapper>